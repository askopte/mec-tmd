import os
import sys
import matplotlib

def main():

    pa = parameters.Parameters()

    pg_resume = None
    v_resume = None
    q_resume = None
    log = None

    render = False

    pa.compute_dependent_parameters()

    pg_re.launch(pa, pg_resume, render, end='all_done')

     # slow_down_cdf.launch(pa, pg_resume, render, True)

if __name__ == '__main__':
    main()